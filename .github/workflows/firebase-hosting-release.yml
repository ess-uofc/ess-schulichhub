# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on Release
'on': release
jobs:
    tag_release:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.OS }}-build-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: |
                      ${{ runner.OS }}-build-${{ env.cache-name }}-
                      ${{ runner.OS }}-build-
                      ${{ runner.OS }}-
            - name: Checkout
              uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}
              env:
                  GITHUB_TOKEN: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PROJECT_HUB_116D7 }}
            - name: Yarn install
              run: yarn install
            - name: Build
              run: yarn run build
            - name: Deploy to Firebase
              uses: FirebaseExtended/action-hosting-deploy@v0
              with:
                  target: live
                  repoToken: '${{ secrets.GITHUB_TOKEN }}'
                  firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PROJECT_HUB_116D7 }}'
                  channelId: live
                  projectId: project-hub-116d7
              env:
                  FIREBASE_CLI_PREVIEWS: hostingchannels
